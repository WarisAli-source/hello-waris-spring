name: demo-ci env

on :
    workflow_dispatch


jobs:
    build:
        runs-on: ubuntu-latest

        steps:
            - name: Say Hello
              run: echo Hello, world! Second workflow
            - name: Install dependencies
              env:
                APP_ENV: demo
              run: echo "Environment variable APP_ENV is set to $APP_ENV"     
