name: demo-ci env

on :
    workflow_dispatch


jobs:
    build:
        runs-on: ubuntu-latest

        steps:
            - name: Say Hello
              run: echo Hello, world! Second workflow
            - name: Install dependencies
              env:
                APP_ENV: DEVELOPMENT
                DB_PASSWORD: ${{ secrets.DB_PASSWORD }}
              run: echo "Environment variable DB_PASSWORD is set to $DB_PASSWORD"     
